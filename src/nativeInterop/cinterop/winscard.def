language = Objective-C
package = au.id.micolous.kotlin.pcsc.internal
modules = PCSC
compilerOpts = -framework PCSC
linkerOpts = -framework PCSC

noStringConversion = SCardListReaders

# headers = winscard.h
# headerFilter = winscard.h pcsclite.h
# compilerOpts.osx = -framework PCSCContext
# linkerOpts.osx = -framework PCSCContext
# linkerOpts.linux = -L/usr/lib64 -L/usr/lib/x86_64-linux-gnu -lpcsclite
# linkerOpts.mingw = -lWinSCard
